# backend/.env.example
# Kopyala: cp backend/.env.example backend/.env  (gercek .env asla commit edilmez)
DATABASE_URL=postgresql+asyncpg://hotrolling:changeme@localhost:5433/hotrolling
DATABASE_URL_SYNC=postgresql+psycopg2://hotrolling:changeme@localhost:5433/hotrolling
MODEL_REGISTRY_PATH=./model_registry
DEFAULT_TICK_MS=1000
